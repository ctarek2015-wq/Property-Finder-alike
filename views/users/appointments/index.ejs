<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<%- include('../../partials/_navbar.ejs') %>

<body>
  <h1>Welcome <%= user.username %></h1>
  <h2>Your Appointments:</h2>

  <% if(viewings.length === 0){ %>
  <p>You have no appointments scheduled.</p>
  <% } %>

  <% if(user.role === 'seeker'){ %>
  <% viewings.forEach(viewing => { %>
  <fieldset>
    <legend>Viewing for <%= viewing.propertyId.title %></legend>
    <p>Appointment Date: <%= viewing.appointmentDate.toISOString().split('T')[0] %></p>
    <p>Appointment Time: <%= viewing.appointmentTime %></p>
    <p>Status: <%= viewing.status %></p>
    <p>Property Owner: <%= viewing.propertyId.owner.username %></p>
    <a href="/properties/<%= viewing.propertyId._id %>"><button>View Property</button></a>
    <a href="/properties/<%= viewing.propertyId._id %>/appointments/<%= viewing._id %>/edit"><button>Edit Appointment</button></a>
  </fieldset>
  <% }) %>
  <% }else %>





</body>

</html>